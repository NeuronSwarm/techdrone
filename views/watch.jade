html
  style.

  body
    canvas#canvas
    script(src='/js/point.js')
    script(src='/js/canvas_state.js')
    script(src='/js/array_plugin.js')
    script(src='https://code.jquery.com/jquery-3.2.1.min.js'
          integrity='sha256-hwg4gsxgFZhOsEEamdOYGBf13FyQuiTwlAQgxVSNgt4='
          crossorigin='anonymous')
    script(src='/js/bezier/spectator.js')
    script.
      var canvasState = new CanvasState();
      var spectator = new Spectator();
      
      var canvas = document.getElementById('canvas');
      canvas.width = window.innerWidth
      canvas.height = window.innerHeight
      var ctx = canvas.getContext ( "2d" );

      spectator.addPlugin(canvasState, ctx);

      ctx.save ();
      var radius = 5;
      var counter = 0;
      var bezierStartPoint;
      function Point(x,y){
        if (arguments.length == 2) {
          this.x = x || 0;
          this.y = y || 0;
        } else {// Point(e)
          e = x; // e: Event
          this.x = e.clientX - 9 || 0;
          this.y = e.clientY - 10 || 0;
        }
      }

      DrawAll = function(){
        Clear();
        myShapes.forEach(function(circle){
          circle.draw();
        })
        canvasState.curves.forEach(function(curve){
          curve.draw(ctx);
        })
      }



      $(document).ready(function() {

      });